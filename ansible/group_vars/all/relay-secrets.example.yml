# Copy this file to `ansible/group_vars/all/relay-secrets.yml` and fill values.
# Keep secrets out of source control (this repo does not track relay-secrets.yml).

relay_database_password: REPLACE_ME
relay_app_secret: REPLACE_ME
relay_operator_token: REPLACE_ME
relay_certbot_email: REPLACE_ME
