# Copy this file to `ansible/host_vars/dev/relay-secrets.yml` and fill values.
# Keep secrets out of source control.
# For Docker Compose dev operations, keep runtime Relay/PostgreSQL secrets
# only in the remote `/opt/agentunnel/compose/.env`.

# Required for private GHCR Relay image pulls.
relay_ghcr_token: ""
