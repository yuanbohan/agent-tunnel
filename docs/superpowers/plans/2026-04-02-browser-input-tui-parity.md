# Browser Input TUI Parity Implementation Plan

**Status:** Cancelled

This plan is no longer active.

We are not adding `input_filter` logic to the repo. The current direction is to keep the transport path byte-transparent and let clients send intended input as-is.
